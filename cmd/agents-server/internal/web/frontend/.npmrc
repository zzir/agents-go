; package-lock.json is deliberately gitignored — direct deps stay exact-pinned instead.
save-exact=true
; overrides.vite pins every vite edge to the root's version: a fresh resolve otherwise walks
; vite 8's optional @vitejs/devtools peer set, which crashes npm's arborist (null edgesOut).
