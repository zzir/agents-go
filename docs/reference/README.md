# Reference

For a reader who wants to **look a fact up**: accurate, dry, scannable. Section
numbers here are permanent addresses — code comments cite them (`spec §2.7f`),
so they are never renumbered and never reused.

The pages are listed below. The map of all four kinds of documentation is in
[`docs/`](../).
